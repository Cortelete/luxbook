/**
 * O cliente Supabase foi desativado.
 * A aplicação não se conecta mais a um banco de dados Supabase.
 * A autenticação e os dados do usuário agora são gerenciados localmente no frontend.
 */
export const initError = "Supabase client is disabled.";
export const supabase = null;
