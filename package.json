{
  "name": "luxury-academy-app",
  "version": "1.0.0",
  "private": true,
  "description": "Project configuration for Vercel deployment.",
  "scripts": {
    "dev": "echo \"This command is a placeholder. Run index.html in a local server for frontend development.\"",
    "build": "echo \"No frontend build step needed. Vercel will build serverless functions automatically.\""
  },
  "dependencies": {
    "@google/genai": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "@vercel/node": "^3.0.25",
    "typescript": "^5.3.3"
  }
}