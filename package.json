{
  "name": "luxury-academy-app",
  "version": "1.0.0",
  "private": true,
  "description": "Project configuration for Vercel deployment.",
  "scripts": {
    "dev": "echo \"This project requires a build step. Run 'npm run build' and serve the 'public' directory.\"",
    "build": "mkdir -p public && cp index.html public/ && cp -r data/ public/data/ && esbuild index.tsx --bundle --minify --outfile=public/index.js --jsx=automatic"
  },
  "dependencies": {
    "@google/genai": "^1.10.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@supabase/supabase-js": "^2.44.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vercel/node": "^3.0.25",
    "typescript": "^5.3.3",
    "esbuild": "^0.21.4"
  }
}